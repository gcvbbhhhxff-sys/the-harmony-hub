"use client";
import { useMemo,useState } from "react";
import { BottomSheet } from "@/components/ui/bottom-sheet";import { Button } from "@/components/ui/button";import { Checkbox } from "@/components/ui/checkbox";import { Dialog } from "@/components/ui/dialog";import { Textarea } from "@/components/ui/textarea";import { useCartStore } from "@/lib/cart/store";
import type { Product } from "@/types/menu";import type { CartAddon,CartItem,CartSelection,MenuAddon,MenuOption,MenuOptionGroup } from "@/types/cart";
type Props={product:Product;groups:MenuOptionGroup[];options:MenuOption[];addons:MenuAddon[];availableAddonIds:string[];open:boolean;onClose:()=>void};
export function ProductDetail({product,groups,options,addons,availableAddonIds,open,onClose}:Props){const addItem=useCartStore(s=>s.addItem);const [selected,setSelected]=useState<Record<string,string[]>>({});const [selectedAddons,setSelectedAddons]=useState<string[]>([]);const [observation,setObservation]=useState("");const [quantity,setQuantity]=useState(1);const chosenOptions=Object.entries(selected).flatMap(([groupId,ids])=>ids.map(id=>options.find(o=>o.id===id)).filter((o):o is MenuOption=>Boolean(o)).map(o=>({optionId:o.id,groupId,nome:o.nome,precoExtra:o.preco_extra} as CartSelection));const chosenAddons=selectedAddons.map(id=>addons.find(a=>a.id===id)).filter((a):a is MenuAddon=>Boolean(a)).map(a=>({addonId:a.id,nome:a.nome,preco:a.preco} as CartAddon));const valid=groups.every(g=>{const count=selected[g.id]?.length??0;return (!g.obrigatorio||count>=g.min_select)&&count<=g.max_select});const unitPrice=useMemo(()=>product.preco+chosenOptions.reduce((s,o)=>s+o.precoExtra,0)+chosenAddons.reduce((s,a)=>s+a.preco,0),[product.preco,chosenOptions,chosenAddons]);const toggle=(g:MenuOptionGroup,id:string)=>setSelected(prev=>{const current=prev[g.id]??[];if(current.includes(id))return {...prev,[g.id]:current.filter(v=>v!==id)};if(current.length>=g.max_select)return prev;return {...prev,[g.id]:[...current,id]}});const confirm=()=>{if(!valid)return;const item:CartItem={id:crypto.randomUUID(),product,options:chosenOptions,addons:chosenAddons,observation,quantity,unitPrice};addItem(item);onClose();};const content=<div className="grid gap-5">{product.imagem_url&&<img src={product.imagem_url} alt={product.nome} className="aspect-[4/3] w-full rounded-lg object-cover"/>}<div><h2 className="text-2xl font-extrabold">{product.nome}</h2>{product.descricao&&<p className="mt-1 text-sm opacity-70">{product.descricao}</p>}</div>{groups.map(g=>{const multi=g.max_select>1||g.min_select!==1;return <fieldset key={g.id} className="grid gap-2"><legend className="font-semibold">{g.nome} {g.obrigatorio&&<span className="text-[var(--color-danger)]">*</span>}</legend>{options.filter(o=>o.group_id===g.id).map(o=><label key={o.id} className="flex items-center justify-between gap-3 rounded-md border p-3 text-sm"><span>{o.nome}{o.preco_extra>0?` (+ R$ ${o.preco_extra.toFixed(2).replace(".",",")})`:""}</span>{multi?<Checkbox checked={(selected[g.id]??[]).includes(o.id)} onChange={()=>toggle(g,o.id)}/>:<input type="radio" name={`group-${g.id}`} checked={(selected[g.id]??[]).includes(o.id)} onChange={()=>setSelected(prev=>({...prev,[g.id]:[o.id]}))} className="accent-[var(--color-primary)]"/>}</label>)}</fieldset>})}{availableAddonIds.length>0&&<fieldset className="grid gap-2"><legend className="font-semibold">Adicionais</legend>{addons.filter(a=>availableAddonIds.includes(a.id)).map(a=><label key={a.id} className="flex items-center justify-between gap-3 rounded-md border p-3 text-sm"><span>{a.nome} (+ R$ {a.preco.toFixed(2).replace(".",",")})</span><Checkbox checked={selectedAddons.includes(a.id)} onChange={()=>setSelectedAddons(v=>v.includes(a.id)?v.filter(id=>id!==a.id):[...v,a.id])}/></label>)}</fieldset>}<Textarea value={observation} onChange={e=>setObservation(e.target.value)} placeholder="Observação do item"/><div className="flex items-center justify-between gap-4"><div className="flex items-center gap-2"><Button variant="outline" size="sm" onClick={()=>setQuantity(q=>Math.max(1,q-1))}>−</Button><span className="min-w-6 text-center">{quantity}</span><Button variant="outline" size="sm" onClick={()=>setQuantity(q=>q+1)}>+</Button></div><strong className="text-lg text-[var(--color-primary)]">R$ {(unitPrice*quantity).toFixed(2).replace(".",",")}</strong></div><Button size="lg" disabled={!valid} onClick={confirm} className="w-full">Adicionar ao carrinho</Button></div>;return <><div className="hidden md:block"><Dialog open={open} onClose={onClose} title="Personalizar pedido">{content}</Dialog></div><div className="md:hidden"><BottomSheet open={open} onClose={onClose} title="Personalizar pedido">{content}</BottomSheet></div></>}
