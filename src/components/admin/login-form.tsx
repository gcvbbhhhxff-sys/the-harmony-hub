"use client";
import { useState,useTransition } from "react";
import { adminLogin } from "@/server/actions/admin-auth";
import { Button } from "@/components/ui/button"; import { Card } from "@/components/ui/card"; import { Input } from "@/components/ui/input";
export function LoginForm(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [pending,startTransition]=useTransition();return <main className="min-h-screen grid place-items-center p-4 bg-[var(--color-secondary)]"><Card className="w-full max-w-md"><p className="text-xs uppercase tracking-[0.18em] text-[var(--color-primary)]">Área administrativa</p><h1 className="mt-2 text-2xl font-extrabold">Entrar</h1><div className="mt-5 grid gap-3"><Input type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><Input type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)}/><Button disabled={pending} onClick={()=>startTransition(async()=>{const r=await adminLogin(email,password);if(!r?.ok)setMessage(r.message||"Não foi possível entrar.")})}>{pending?"Entrando…":"Entrar"}</Button>{message&&<p className="text-sm text-[var(--color-danger)]">{message}</p>}</div></Card></main>}
