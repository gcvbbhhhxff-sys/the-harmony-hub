"use client";
import { useState,useTransition } from "react";
import { deleteAddon,deleteCategory,deleteOption,deleteOptionGroup,linkAddon,saveAddon,saveCategory,saveOption,saveOptionGroup,saveProduct,toggleProduct,unlinkAddon,uploadProductImage } from "@/server/actions/admin-menu";
import { Button } from "@/components/ui/button";import { Card } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Checkbox } from "@/components/ui/checkbox";
type Category={id:string;nome:string;ordem:number;ativo:boolean};type Product={id:string;category_id:string;nome:string;descricao:string|null;preco:number;imagem_url:string|null;ativo:boolean;destaque:boolean};type Group={id:string;product_id:string;nome:string;min_select:number;max_select:number;obrigatorio:boolean;ordem:number};type Option={id:string;group_id:string;nome:string;preco_extra:number;ordem:number;ativo:boolean};type Addon={id:string;nome:string;preco:number;ativo:boolean};type Link={product_id:string;addon_id:string};
export default function MenuAdminClient({initial}:{initial:{categories:Category[];products:Product[];groups:Group[];options:Option[];addons:Addon[];links:Link[]}}){const[data,setData]=useState(initial);const[msg,setMsg]=useState("");const[pending,startTransition]=useTransition();const[editingProduct,setEditingProduct]=useState<Product|null>(null);const[cat,setCat]=useState({nome:"",ordem:0});const[prod,setProd]=useState({category_id:initial.categories[0]?.id??"",nome:"",descricao:"",preco:0,ativo:true,destaque:false});const[group,setGroup]=useState({product_id:initial.products[0]?.id??"",nome:"",min_select:1,max_select:1,obrigatorio:true,ordem:0});const[opt,setOpt]=useState({group_id:initial.groups[0]?.id??"",nome:"",preco_extra:0,ordem:0,ativo:true});const[addon,setAddon]=useState({nome:"",preco:0,ativo:true});const[linkProduct,setLinkProduct]=useState(initial.products[0]?.id??"");const[linkAddonId,setLinkAddonId]=useState(initial.addons[0]?.id??"");const run=(fn:()=>Promise<{ok:boolean;message?:string}>)=>startTransition(async()=>{const r=await fn();setMsg(r.ok?"Operação concluída.":r.message||"Erro.")});const reloadLocal=()=>setTimeout(()=>window.location.reload(),350);const saveProd=()=>run(async()=>{const r=await saveProduct(editingProduct?{...prod,id:editingProduct.id}:prod);if(r.ok){setEditingProduct(null);reloadLocal()}return r});return <main className="p-4 md:p-6"><div className="flex items-center justify-between"><h1 className="text-3xl font-extrabold">Cardápio</h1><span className="text-sm opacity-60">{data.products.length} produtos</span></div><div className="mt-6 grid gap-5 lg:grid-cols-2"><Card><h2 className="font-bold">Categorias</h2><div className="mt-3 grid gap-2"><Input placeholder="Nome" value={cat.nome} onChange={e=>setCat({...cat,nome:e.target.value})}/><Input type="number" value={cat.ordem} onChange={e=>setCat({...cat,ordem:Number(e.target.value)})}/><Button disabled={pending} onClick={()=>run(async()=>{const r=await saveCategory({...cat,ativo:true});if(r.ok)reloadLocal();return r})}>Criar categoria</Button></div><div className="mt-4 grid gap-2">{data.categories.map(c=><div key={c.id} className="flex items-center justify-between gap-2 rounded border p-2 text-sm"><span>{c.nome} · ordem {c.ordem}</span><div className="flex gap-1"><Button size="sm" variant="outline" onClick={()=>run(()=>saveCategory({...c,ativo:!c.ativo}))}>{c.ativo?"Desativar":"Ativar"}</Button><Button size="sm" variant="destructive" onClick={()=>window.confirm("Excluir categoria?")&&run(async()=>{const r=await deleteCategory(c.id);if(r.ok)reloadLocal();return r})}>Excluir</Button></div></div>)}</div></Card><Card><h2 className="font-bold">{editingProduct?"Editar produto":"Novo produto"}</h2><div className="mt-3 grid gap-2"><select className="h-10 rounded-md border px-3" value={prod.category_id} onChange={e=>setProd({...prod,category_id:e.target.value})}>{data.categories.map(c=><option key={c.id} value={c.id}>{c.nome}</option>)}</select><Input placeholder="Nome" value={prod.nome} onChange={e=>setProd({...prod,nome:e.target.value})}/><Input placeholder="Descrição" value={prod.descricao} onChange={e=>setProd({...prod,descricao:e.target.value})}/><Input type="number" min="0" step="0.01" value={prod.preco} onChange={e=>setProd({...prod,preco:Number(e.target.value)})}/><label className="flex gap-2 text-sm"><Checkbox checked={prod.ativo} onChange={e=>setProd({...prod,ativo:e.target.checked})}/> Disponível</label><label className="flex gap-2 text-sm"><Checkbox checked={prod.destaque} onChange={e=>setProd({...prod,destaque:e.target.checked})}/> Destaque</label><div className="flex gap-2"><Button disabled={pending} onClick={saveProd}>{editingProduct?"Salvar alterações":"Criar produto"}</Button>{editingProduct&&<Button variant="ghost" onClick={()=>setEditingProduct(null)}>Cancelar</Button>}</div></div></Card><Card><h2 className="font-bold">Grupos e opções</h2><div className="mt-3 grid gap-2"><select className="h-10 rounded-md border px-3" value={group.product_id} onChange={e=>setGroup({...group,product_id:e.target.value})}>{data.products.map(p=><option key={p.id} value={p.id}>{p.nome}</option>)}</select><Input placeholder="Nome do grupo" value={group.nome} onChange={e=>setGroup({...group,nome:e.target.value})}/><div className="grid grid-cols-2 gap-2"><Input type="number" min="0" value={group.min_select} onChange={e=>setGroup({...group,min_select:Number(e.target.value)})}/><Input type="number" min="0" value={group.max_select} onChange={e=>setGroup({...group,max_select:Number(e.target.value)})}/></div><label className="flex gap-2 text-sm"><Checkbox checked={group.obrigatorio} onChange={e=>setGroup({...group,obrigatorio:e.target.checked})}/> Obrigatório</label><Button onClick={()=>run(()=>saveOptionGroup(group))}>Criar grupo</Button><div className="mt-2 grid gap-2">{data.groups.map(g=><div key={g.id} className="rounded border p-2 text-sm"><div className="flex justify-between"><span>{g.nome}</span><Button size="sm" variant="destructive" onClick={()=>run(()=>deleteOptionGroup(g.id))}>Excluir</Button></div><div className="mt-2 grid gap-1">{data.options.filter(o=>o.group_id===g.id).map(o=><div key={o.id} className="flex items-center justify-between"><span>{o.nome} · +R$ {Number(o.preco_extra).toFixed(2).replace(".",",")}</span><Button size="sm" variant="ghost" onClick={()=>run(()=>deleteOption(o.id))}>Excluir</Button></div>)}</div></div>)}</div></div></Card><Card><h2 className="font-bold">Adicionar opção</h2><div className="mt-3 grid gap-2"><select className="h-10 rounded-md border px-3" value={opt.group_id} onChange={e=>setOpt({...opt,group_id:e.target.value})}>{data.groups.map(g=><option key={g.id} value={g.id}>{g.nome}</option>)}</select><Input placeholder="Nome" value={opt.nome} onChange={e=>setOpt({...opt,nome:e.target.value})}/><Input type="number" min="0" step="0.01" value={opt.preco_extra} onChange={e=>setOpt({...opt,preco_extra:Number(e.target.value)})}/><Button onClick={()=>run(()=>saveOption(opt))}>Criar opção</Button></div></Card><Card><h2 className="font-bold">Adicionais</h2><div className="mt-3 grid gap-2"><Input placeholder="Nome" value={addon.nome} onChange={e=>setAddon({...addon,nome:e.target.value})}/><Input type="number" min="0" step="0.01" value={addon.preco} onChange={e=>setAddon({...addon,preco:Number(e.target.value)})}/><Button onClick={()=>run(()=>saveAddon(addon))}>Criar adicional</Button>{data.addons.map(a=><div key={a.id} className="flex items-center justify-between rounded border p-2 text-sm"><span>{a.nome} · R$ {Number(a.preco).toFixed(2).replace(".",",")}</span><Button size="sm" variant="destructive" onClick={()=>run(()=>deleteAddon(a.id))}>Excluir</Button></div>)}<div className="border-t pt-3"><select className="h-10 w-full rounded-md border px-3" value={linkProduct} onChange={e=>setLinkProduct(e.target.value)}>{data.products.map(p=><option key={p.id} value={p.id}>{p.nome}</option>)}</select><select className="mt-2 h-10 w-full rounded-md border px-3" value={linkAddonId} onChange={e=>setLinkAddonId(e.target.value)}>{data.addons.map(a=><option key={a.id} value={a.id}>{a.nome}</option>)}</select><Button className="mt-2 w-full" onClick={()=>run(()=>linkAddon(linkProduct,linkAddonId))}>Associar adicional</Button></div></div></Card></div><div className="mt-6 grid gap-3">{data.products.map(p=><Card key={p.id}><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="font-semibold">{p.nome}</p><p className="text-xs opacity-55">R$ {Number(p.preco).toFixed(2).replace(".",",")}</p></div><div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={()=>{setEditingProduct(p);setProd({category_id:p.category_id,nome:p.nome,descricao:p.descricao??"",preco:Number(p.preco),ativo:p.ativo,destaque:p.destaque})}}>Editar</Button><Button size="sm" variant="outline" onClick={()=>run(()=>toggleProduct(p.id,!p.ativo))}>{p.ativo?"Indisponível":"Ativar"}</Button><Button size="sm" variant="destructive" onClick={()=>window.confirm("Excluir produto?")&&run(()=>deleteProduct(p.id))}>Excluir</Button><label className="inline-flex min-h-9 cursor-pointer items-center rounded-md border px-3 text-sm">Imagem<input type="file" accept="image/*" className="hidden" onChange={e=>{const file=e.target.files?.[0];if(file)run(()=>{const f=new FormData();f.append("productId",p.id);f.append("file",file);return uploadProductImage(f)})}}/></label></div></div><div className="mt-2 text-xs opacity-55">{data.links.filter(l=>l.product_id===p.id).map(l=>data.addons.find(a=>a.id===l.addon_id)?.nome).filter(Boolean).join(", ")||"Sem adicionais associados"}</div></Card>)}</div>{msg&&<p className="mt-4 text-sm">{msg}</p>}</main>}
