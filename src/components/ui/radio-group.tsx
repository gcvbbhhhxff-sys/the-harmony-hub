type RadioGroupProps = { name: string; options: { value: string; label: string }[]; value?: string; onChange?: (value: string) => void };
export function RadioGroup({ name, options, value, onChange }: RadioGroupProps) { return <div role="radiogroup" className="grid gap-2">{options.map((option) => <label key={option.value} className="flex items-center gap-2 text-sm"><input type="radio" name={name} value={option.value} checked={value === option.value} onChange={(event) => onChange?.(event.target.value)} className="accent-[var(--color-primary)]" />{option.label}</label>)}</div>; }
