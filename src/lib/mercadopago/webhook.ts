import { createHmac, timingSafeEqual } from "node:crypto";
export function validateMercadoPagoSignature(signature:string|null,requestId:string|null,dataId:string|null,secret:string){if(!signature||!secret)return false;const parts=Object.fromEntries(signature.split(",").map(p=>{const [k,...v]=p.split("=");return [k,v.join("=")]}));const ts=parts.ts;const v1=parts.v1;if(!ts||!v1)return false;const pairs:string[]=[];if(dataId)pairs.push(`id:${dataId}`);if(requestId)pairs.push(`request-id:${requestId}`);pairs.push(`ts:${ts}`);const manifest=`${pairs.join(";")};`;const expected=createHmac("sha256",secret).update(manifest).digest("hex");const a=Buffer.from(expected,"utf8");const b=Buffer.from(v1,"utf8");return a.length===b.length&&timingSafeEqual(a,b);}
