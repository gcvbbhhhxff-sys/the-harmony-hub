import { createClient } from "@/lib/supabase/server";import { Card } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";export const dynamic="force-dynamic";export default async function AdminDashboard(){const supabase=await createClient();const start=new Date();start.setHours(0,0,0,0);const {data:orders}=await supabase.from("orders").select("id,total,status,status_pagamento,criado_em").gte("criado_em",start.toISOString());const operational=(orders??[]).filter(o=>o.status_pagamento==="confirmado");const revenue=operational.reduce((s,o)=>s+Number(o.total),0);const avg=operational.length?revenue/operational.length:0;return <main className="p-4 md:p-6"><h1 className="text-3xl font-extrabold">Dashboard</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Card><p className="text-sm opacity-60">Pedidos do dia</p><p className="mt-2 text-3xl font-extrabold">{operational.length}</p></Card><Card><p className="text-sm opacity-60">Faturamento</p><p className="mt-2 text-3xl font-extrabold">R$ {revenue.toFixed(2).replace(".",",")}</p></Card><Card><p className="text-sm opacity-60">Pendentes</p><p className="mt-2 text-3xl font-extrabold">{(orders??[]).filter(o=>o.status_pagamento==="pendente").length}</p></Card><Card><p className="text-sm opacity-60">Ticket médio</p><p className="mt-2 text-3xl font-extrabold">R$ {avg.toFixed(2).replace(".",",")}</p></Card></div><Card className="mt-6"><h2 className="font-bold">Pedidos recentes</h2><div className="mt-4 grid gap-2">{(orders??[]).slice(0,8).map(o=><div key={o.id} className="flex items-center justify-between border-b pb-2 text-sm"><span>#{o.id.slice(0,8)}</span><Badge status={o.status==="saiu_para_entrega"?"saiu":o.status as "recebido"}>{o.status.replaceAll("_"," ")}</Badge><span>R$ {Number(o.total).toFixed(2).replace(".",",")}</span></div>)}</div></Card></main>}
