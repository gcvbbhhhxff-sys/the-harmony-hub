import { createClient } from "@/lib/supabase/server";
import { OrdersBoard } from "@/components/admin/orders-board";
export const dynamic="force-dynamic";
export default async function OrdersPage(){const supabase=await createClient();const {data}=await supabase.from("orders").select("id,status,status_pagamento,total,criado_em,customers(nome,telefone)").order("criado_em",{ascending:false});const initial=(data??[]).map(o=>({id:o.id,status:o.status as "recebido"|"preparando"|"saiu_para_entrega"|"entregue"|"cancelado",status_pagamento:o.status_pagamento,total:Number(o.total),criado_em:o.criado_em,customer:Array.isArray(o.customers)?o.customers[0]??null:o.customers}));return <main className="overflow-x-auto p-4 md:p-6"><h1 className="text-3xl font-extrabold">Pedidos</h1><OrdersBoard initial={initial}/></main>}
