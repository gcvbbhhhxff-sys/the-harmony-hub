import { createClient } from "@/lib/supabase/server";import ZoneAdminClient from "@/components/admin/zone-admin-client";export const dynamic="force-dynamic";export default async function DeliveryZonesPage(){const s=await createClient();const {data}=await s.from("delivery_zones").select("id,nome,taxa,ativo").order("nome");return <ZoneAdminClient initial={data??[]}/>}
